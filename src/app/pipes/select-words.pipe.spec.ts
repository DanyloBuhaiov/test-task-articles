import { SelectWordsPipe } from './select-words.pipe';

describe('SelectWordsPipe', () => {
  it('create an instance', () => {
    const pipe = new SelectWordsPipe();
    expect(pipe).toBeTruthy();
  });
});
